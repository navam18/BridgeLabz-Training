# Database connection settings.
# Edit these to match your local MySQL setup.
# NOTE: for a real/shared project, do not commit real passwords here —
# use environment variables instead. This file is fine for local coursework use.

db.url=jdbc:mysql://localhost:3306/health_clinic_db?useSSL=false&serverTimezone=UTC
db.user=clinic_app_user
db.password=StrongPassword123!
